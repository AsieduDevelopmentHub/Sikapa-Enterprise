# Production runtime dependencies (excludes pytest / coverage tooling).
# Used by the hardened Dockerfile; CI and local dev still use requirements.txt.

# -----------------------------
# Core Framework
# -----------------------------
fastapi==0.110.0
uvicorn[standard]==0.23.2
python-multipart==0.0.18

# -----------------------------
# Data Validation & Settings
# -----------------------------
pydantic[email]==2.10.6
pydantic-settings==2.5.2
email-validator==2.2.0
python-dotenv==1.0.1
nh3==0.2.18

# -----------------------------
# Database & Supabase
# -----------------------------
supabase==2.10.0
psycopg2-binary==2.9.10
alembic==1.13.3
sqlmodel==0.0.21

# -----------------------------
# Authentication & Security
# -----------------------------
authlib==1.3.2
cryptography==43.0.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyotp==2.9.0
qrcode[pil]==7.4.2
slowapi==0.1.9

# -----------------------------
# Email Service
# -----------------------------
resend==0.7.2

# -----------------------------
# Caching & Task Queue
# -----------------------------
redis[hiredis]==5.0.8
celery[redis]==5.4.0

# -----------------------------
# Document Generation
# -----------------------------
reportlab==4.0.9

# -----------------------------
# HTTP / async DB
# -----------------------------
httpx==0.27.2
asyncpg==0.27.0

# -----------------------------
# Observability (optional — init skipped when SENTRY_DSN unset)
# -----------------------------
sentry-sdk[fastapi]==2.19.2
