# -----------------------------
# Core Framework
# -----------------------------
fastapi==0.110.0
uvicorn[standard]==0.23.2
python-multipart==0.0.9          # Required for file uploads (UploadFile)

# -----------------------------
# Data Validation & Settings
# -----------------------------
pydantic[email]==2.10.6
pydantic-settings==2.5.2
email-validator==2.2.0
python-dotenv==1.0.1
bleach==6.2.0

# -----------------------------
# Database & Supabase
# -----------------------------
supabase==2.10.0
psycopg2-binary==2.9.10
alembic==1.13.3
sqlmodel==0.0.21

# -----------------------------
# Authentication & Security
# -----------------------------
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4            # Password hashing utilities
bcrypt==4.0.1                     # Pin for passlib 1.7.4 (4.1+ breaks hashing on some Linux runners)
pyotp==2.9.0                      # TOTP 2FA codes (Phase 3)
qrcode[pil]==7.4.2                # QR codes for 2FA setup (Phase 3)
slowapi==0.1.9                    # Rate limiting

# -----------------------------
# Email Service
# -----------------------------
resend==0.7.2                     # Email delivery service

# -----------------------------
# Document Generation
# -----------------------------
reportlab==4.0.9                  # PDF generation for invoices

# -----------------------------
# Development & Testing
# -----------------------------
pytest==8.3.2                     # Testing framework
pytest-asyncio==0.23.8            # Async test support
httpx==0.27.2                     # Test client for FastAPI; also used in ai_insights_service
pytest-cov==5.0.0

asyncpg==0.27.0
aiosqlite==0.20.0                # Async SQLite driver for tests
