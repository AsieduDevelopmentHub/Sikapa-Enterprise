# Canonical Python dependency manifest for Sikapa backend (B-012).
# Docker/CI install from this file. `pyproject.toml` mirrors versions for tooling metadata.
# Production Docker image uses requirements-prod.txt (no pytest).
# Regenerate lock: pip install -r requirements.txt

# -----------------------------
# Core Framework
# -----------------------------
fastapi==0.136.3
starlette==1.2.0                   # Security-patched; requires jinja2 for Sentry Starlette integration
jinja2>=3.1.5
uvicorn[standard]==0.34.0
python-multipart==0.0.27

# -----------------------------
# Data Validation & Settings
# -----------------------------
pydantic[email]==2.10.6
pydantic-settings==2.5.2
email-validator==2.2.0
python-dotenv==1.2.2
nh3==0.2.18                      # HTML sanitization (replaces deprecated bleach)

# -----------------------------
# Database & Supabase
# -----------------------------
supabase==2.10.0
psycopg2-binary==2.9.10
alembic==1.13.3
sqlmodel==0.0.21

# -----------------------------
# Authentication & Security
# -----------------------------
authlib==1.6.11                   # JWT tokens (replaces unmaintained python-jose)
cryptography==46.0.7              # Required by authlib; also provides Fernet for TOTP encryption
passlib[bcrypt]==1.7.4            # Password hashing utilities
bcrypt==4.0.1                     # Pin for passlib 1.7.4 (4.1+ breaks hashing on some Linux runners)
pyotp==2.9.0                      # TOTP 2FA codes
qrcode[pil]==7.4.2                # QR codes for 2FA setup
slowapi==0.1.9                    # Rate limiting

# -----------------------------
# Email Service
# -----------------------------
resend==2.30.1                     # Email delivery service

# -----------------------------
# Caching & Task Queue
# -----------------------------
redis[hiredis]==5.0.8             # Redis client with fast C parser
celery[redis]==5.4.0              # Distributed task queue backed by Redis

# -----------------------------
# Document Generation
# -----------------------------
reportlab==4.0.9                  # PDF generation for invoices

# -----------------------------
# HTTP / async DB
# -----------------------------
httpx==0.27.2                     # Test client for FastAPI; also used in payment clients
asyncpg==0.27.0
requests>=2.33.0                    # Transitive pin — supabase/resend pull older requests

# -----------------------------
# Observability (optional — init skipped when SENTRY_DSN unset)
# -----------------------------
sentry-sdk[fastapi]==2.19.2

# -----------------------------
# Development & Testing (CI + local dev only)
# -----------------------------
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==6.0.0
aiosqlite==0.20.0                # Async SQLite driver for tests
