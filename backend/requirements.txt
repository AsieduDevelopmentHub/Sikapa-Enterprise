# -----------------------------
# Core Framework
# -----------------------------
fastapi==0.110.0
uvicorn[standard]==0.23.2
python-multipart==0.0.18          # Required for file uploads (UploadFile); >=0.0.18 fixes CVE-2024-53981

# -----------------------------
# Data Validation & Settings
# -----------------------------
pydantic[email]==2.10.6
pydantic-settings==2.5.2
email-validator==2.2.0
python-dotenv==1.0.1
nh3==0.2.18                      # HTML sanitization (replaces deprecated bleach)

# -----------------------------
# Database & Supabase
# -----------------------------
supabase==2.10.0
psycopg2-binary==2.9.10
alembic==1.13.3
sqlmodel==0.0.21

# -----------------------------
# Authentication & Security
# -----------------------------
authlib==1.3.2                    # JWT tokens (replaces unmaintained python-jose)
cryptography==43.0.1              # Required by authlib; also provides Fernet for TOTP encryption
passlib[bcrypt]==1.7.4            # Password hashing utilities
bcrypt==4.0.1                     # Pin for passlib 1.7.4 (4.1+ breaks hashing on some Linux runners)
pyotp==2.9.0                      # TOTP 2FA codes
qrcode[pil]==7.4.2                # QR codes for 2FA setup
slowapi==0.1.9                    # Rate limiting

# -----------------------------
# Email Service
# -----------------------------
resend==0.7.2                     # Email delivery service

# -----------------------------
# Caching & Task Queue
# -----------------------------
redis[hiredis]==5.0.8             # Redis client with fast C parser
celery[redis]==5.4.0              # Distributed task queue backed by Redis

# -----------------------------
# Document Generation
# -----------------------------
reportlab==4.0.9                  # PDF generation for invoices

# -----------------------------
# Development & Testing
# -----------------------------
pytest==8.3.2                     # Testing framework
pytest-asyncio==0.23.8            # Async test support
httpx==0.27.2                     # Test client for FastAPI; also used in payment clients
pytest-cov==5.0.0

asyncpg==0.27.0
aiosqlite==0.20.0                # Async SQLite driver for tests
