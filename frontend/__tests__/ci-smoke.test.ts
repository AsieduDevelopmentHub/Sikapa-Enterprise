import { describe, it, expect } from "vitest";

describe("Sikapa Enterprise frontend", () => {
  it("runs Vitest in CI (replace with real tests as features land)", () => {
    expect(1 + 1).toBe(2);
  });
});
